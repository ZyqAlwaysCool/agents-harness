# Base Standard

Base defines repository-level control rules:

- `app/` is the only business-code root
- docs are layered
- script entrypoints are stable
- verification is mandatory
- high-impact changes require human confirmation

Base should stay light and stack-agnostic.

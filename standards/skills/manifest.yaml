version: 1

skills:
  - id: repo-reading
    source: native
    ref: builtin
    when:
      - task_start
      - architecture_review
    required: true
    notes: Use native repository reading and editing capabilities first.

  - id: python-fastapi-pack
    source: local
    ref: standards/python-fastapi
    when:
      - project_bootstrap
      - stack_alignment
    required: true
    notes: Use this pack as the default Python-first structure and verification guide.
